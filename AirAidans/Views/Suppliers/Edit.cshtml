@model AirAidans.DATA.EF.Models.Supplier

@{
    ViewData["Title"] = "Edit";
}

<h1 class="heading-text">Edit</h1>

<div class="container">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <table class="table table-bordered t-text">
                    <tbody>
                        <tr>
                            <th class="text-end" style="width: 30%;">Supplier Name:</th>
                            <td>
                                <input asp-for="SupplierName" class="form-control" />
                                <span asp-validation-for="SupplierName" class="text-danger"></span>
                            </td>
                        </tr>
                        <tr>
                            <th class="text-end">Town:</th>
                            <td>
                                <input asp-for="Town" class="form-control" />
                                <span asp-validation-for="Town" class="text-danger"></span>
                            </td>
                        </tr>
                        <tr>
                            <th class="text-end">Country:</th>
                            <td>
                                <input asp-for="Country" class="form-control" />
                                <span asp-validation-for="Country" class="text-danger"></span>
                            </td>
                        </tr>
                    </tbody>
                </table>

                <div class="form-group text-center">
                    <a asp-action="Index" class="btn btn-danger">Back To List</a>
                    <button type="submit" class="btn btn-primary">Save</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
